# Test Cases - Sulaiman (Backend)

## clarificationScopeD.test.js

| Test ID | Test Name | Description | Expected Outcome |
|---|---|---|---|
| BE-SU-001 | 404s when the tender does not exist (UC-D1) | Calls `POST /api/tenders/999999/clarification-logs/detect-deviation` with a `ma_staff` token for a tender id that was never created. | Response status is 404. |
| BE-SU-002 | 409s when the tender has no alternative_offer_price (UC-D1) | Calls detect-deviation as `ma_staff` on `tenderNoAlt`, a tender created with `alternative_offer_price: null`. | Response status is 409. |
| BE-SU-003 | Blocks a non ma_staff user (403) (UC-D1) | Calls detect-deviation on the flagged tender using an `evaluator` token instead of `ma_staff`. | Response status is 403. |
| BE-SU-004 | Blocks an unauthenticated request (401) (UC-D1) | Calls detect-deviation on the flagged tender with no `Authorization` header at all. | Response status is 401. |
| BE-SU-005 | Flags a deviation that exceeds tolerance and generates a deterministic AI rationale (UC-D1) | Calls detect-deviation as `ma_staff` on `tenderFlagged` (main offer 100000, alt offer 94000, i.e. 6% below main). | Response status is 201; `status` is `flagged`, `log_type` is `pricing_deviation`, `deviation_percentage` is 6, and `ai_rationale` equals "Alternative offer is 6% below the main offer, exceeding the 4% tolerance threshold." |
| BE-SU-006 | Blocks a second active pricing_deviation log for the same tender (409) (UC-D1) | Immediately re-runs detect-deviation as `ma_staff` on the same `tenderFlagged`, which already has an active flagged log from the previous test. | Response status is 409. |
| BE-SU-007 | Logs a deviation within tolerance as no_action_required (200, not 201) (UC-D1) | Calls detect-deviation as `ma_staff` on `tenderNoAction` (main offer 100000, alt offer 97000, i.e. 3% below main, within the 4% tolerance). | Response status is 200 (not 201); `status` is `no_action_required`, `deviation_percentage` is 3, and `ai_rationale` equals "Alternative offer is 3% below the main offer, within the 4% tolerance threshold - no vendor follow-up required." |
| BE-SU-008 | Lists logs with a pagination envelope (UC-D6) | Calls `GET /api/clarification-logs` as `evaluator` after the UC-D1 logs have been created. | Response status is 200; `data` is an array and `pagination.total` is at least 2. |
| BE-SU-009 | Filters by tender_id (UC-D6) | Calls the list endpoint with `?tender_id=<tenderFlagged.id>` as `evaluator`. | Response status is 200 and every returned log's `tender_id` matches `tenderFlagged.id`. |
| BE-SU-010 | Filters by status (UC-D6) | Calls the list endpoint with `?status=no_action_required` as `evaluator`. | Response status is 200, every returned log has `status` of `no_action_required`, and the earlier `no_action_required` log's id is present in the results. |
| BE-SU-011 | Rejects an invalid status filter with 400 (UC-D6) | Calls the list endpoint with `?status=not_a_real_status`. | Response status is 400. |
| BE-SU-012 | 404s for a nonexistent log (UC-D6) | Calls `GET /api/clarification-logs/999999` as `evaluator`. | Response status is 404. |
| BE-SU-013 | Returns full detail with empty messages/job_adjustment_requests arrays for a fresh log (UC-D6) | Calls `GET /api/clarification-logs/:id` for the freshly created `no_action_required` log, which has no messages or JARs yet. | Response status is 200; `messages` and `job_adjustment_requests` both equal empty arrays. |
| BE-SU-014 | Blocks a non ma_staff/vendor_liaison user from drafting (403) (UC-D2) | Calls `POST /api/clarification-logs/:id/draft-message` on the flagged log using an `evaluator` token. | Response status is 403. |
| BE-SU-015 | 409s when the log status is not flagged (UC-D2) | Calls draft-message as `ma_staff` on the `no_action_required` log (status is not `flagged`). | Response status is 409. |
| BE-SU-016 | Drafts a deterministic AI message and moves the log to draft_ready (UC-D2) | Calls draft-message as `vendor_liaison` on the flagged log for `TC-D-001`. | Response status is 201; `message_type` is `draft`, `ai_generated` is `true`, `subject` is "Clarification Request - TC-D-001 Pricing Deviation", and `body` contains "Vendor Flagged Pte Ltd", "6%" and "Kindly confirm or justify this deviation within 5 business days."; a follow-up GET on the log shows `status` of `draft_ready`. |
| BE-SU-017 | 409s drafting again now the log is no longer flagged (UC-D2) | Re-calls draft-message as `ma_staff` on the same log, now in `draft_ready` status. | Response status is 409. |
| BE-SU-018 | Edits the draft body/subject (UC-D2) | Calls `PATCH /api/clarification-messages/:id` as `ma_staff` with a new `subject` and `body` for the draft message. | Response status is 200; `subject` is "Edited subject" and `body` is "Edited body text." |
| BE-SU-019 | Rejects an edit with an empty body (400) (UC-D2) | Calls the same PATCH endpoint with `body: ''`. | Response status is 400. |
| BE-SU-020 | 404s editing a nonexistent message (UC-D2) | Calls the PATCH endpoint against message id 999999. | Response status is 404. |
| BE-SU-021 | 409s approving a vendor_response message (only drafts are approvable) (UC-D2) | Directly creates a `ClarificationLog`/`ClarificationMessage` fixture with `message_type: 'vendor_response'`, then calls `POST /api/clarification-messages/:id/approve` as `ma_staff` on it. | Response status is 409. |
| BE-SU-022 | Approves the draft and moves the log to approved (UC-D2) | Calls approve as `vendor_liaison` on the edited draft message. | Response status is 200; `approved_by` and `approved_at` are not null; a follow-up GET on the log shows `status` of `approved`. |
| BE-SU-023 | 409s approving the same draft again (log no longer draft_ready) (UC-D2) | Re-calls approve on the same draft message as `ma_staff`. | Response status is 409. |
| BE-SU-024 | 409s sending when the log is not approved (UC-D2) | Calls `POST /api/clarification-logs/:id/send` as `ma_staff` on the `no_action_required` log (status is not `approved`), with `dispatch_channel: 'email'`. | Response status is 409. |
| BE-SU-025 | 400s sending without a dispatch_channel (UC-D2) | Calls send on the approved flagged log with an empty request body. | Response status is 400. |
| BE-SU-026 | 400s sending with an invalid dispatch_channel (UC-D2) | Calls send on the approved flagged log with `dispatch_channel: 'carrier_pigeon'`. | Response status is 400. |
| BE-SU-027 | Sends the approved message and sets follow_up_due_at ~5 days out (UC-D2) | Calls send on the approved flagged log with `dispatch_channel: 'email'`. | Response status is 200; `message_type` is `sent`, `body` is "Edited body text.", `dispatch_channel` is `email`; a follow-up GET shows the log `status` of `sent` and `follow_up_due_at` matching a `YYYY-MM-DD` date pattern. |
| BE-SU-028 | Runs detection, drafts and approves normally (UC-D2 edge case: vendor missing contact info) | Runs detect-deviation, draft-message and approve in sequence as `ma_staff` for `tenderNoVendorName`, whose `vendor_name` is an empty string. | Each step returns the expected success status (201, 201, 200 respectively), establishing a log/draft/approval that has no vendor contact info on file. |
| BE-SU-029 | 409s sending because the vendor has no contact information on file (UC-D2 edge case) | Calls send on the approved log for `tenderNoVendorName` with `dispatch_channel: 'email'`. | Response status is 409 and the response message matches "no contact information". |
| BE-SU-030 | 409s recording a response unless the log is sent (UC-D5) | Calls `POST /api/clarification-logs/:id/responses` as `ma_staff` on the `no_action_required` log (status is not `sent`). | Response status is 409. |
| BE-SU-031 | 400s recording a response without response_notes (UC-D5) | Calls the responses endpoint on the sent flagged log as `vendor_liaison` with only a `body`, omitting `response_notes`. | Response status is 400. |
| BE-SU-032 | Records a vendor response and moves the log to responded (UC-D5) | Calls the responses endpoint on the sent flagged log as `vendor_liaison` with `subject`, `body` and `response_notes`. | Response status is 201 with `message_type` of `vendor_response`; a follow-up GET shows the log `status` of `responded` and `response_notes` equal to the submitted value. |
| BE-SU-033 | 400s an attachment upload with no file attached (UC-D5) | Calls `POST /api/clarification-messages/:id/attachments` on the vendor response message without attaching a file. | Response status is 400. |
| BE-SU-034 | 404s attaching to a nonexistent message (UC-D5) | Calls the attachments endpoint against message id 999999 with a file attached. | Response status is 404. |
| BE-SU-035 | 409s attaching a file to a non vendor_response message (UC-D5) | Calls the attachments endpoint on the "no vendor" draft message (a `draft`, not a `vendor_response`), with a file attached. | Response status is 409. |
| BE-SU-036 | 502s when the Cloudinary upload fails (UC-D5) | Mocks `cloudinaryService.uploadBuffer` to reject with an error, then attaches a file to the vendor response message. | Response status is 502. |
| BE-SU-037 | Uploads an attachment to the vendor response message (UC-D5) | Mocks `cloudinaryService.uploadBuffer` to resolve with a Cloudinary result, then attaches `quotation.pdf` to the vendor response message. | Response status is 201; `original_filename` is "quotation.pdf", `file_url` matches the mocked `secure_url`, and `cloudinaryService.uploadBuffer` was called. |
| BE-SU-038 | Blocks a non vendor_liaison user from creating a JAR (403) (UC-D7) | Calls `POST /api/clarification-logs/:id/job-adjustment-requests` as `ma_staff` (not `vendor_liaison`) on the flagged log. | Response status is 403. |
| BE-SU-039 | 404s when source_message_id does not belong to this log (UC-D7) | Calls the JAR creation endpoint as `vendor_liaison` on the flagged log but passes a `source_message_id` belonging to a different log. | Response status is 404. |
| BE-SU-040 | 409s creating a JAR when the log is not in responded status (UC-D7) | Calls the JAR creation endpoint as `vendor_liaison` on the `tenderNoVendorName` log, which is `approved` (never successfully sent) rather than `responded`, using a valid `source_message_id` belonging to that log. | Response status is 409. |
| BE-SU-041 | Creates a material JAR that is pending_approval (UC-D7) | Calls the JAR creation endpoint as `vendor_liaison` on the responded flagged log with `is_material: true`. | Response status is 201; `is_material` is `true` and `approval_status` is `pending_approval`. |
| BE-SU-042 | Creates a non-material JAR that is auto-approved (UC-D7) | Calls the JAR creation endpoint as `vendor_liaison` on the same log with `is_material: false`. | Response status is 201; `is_material` is `false` and `approval_status` is `approved`. |
| BE-SU-043 | Lists JARs filtered by tender_id and is_material (UC-D7) | Calls `GET /api/job-adjustment-requests?tender_id=<id>&is_material=true` as `ma_staff`. | Response status is 200; exactly one result is returned and its id matches the material JAR created earlier. |
| BE-SU-044 | Lists JARs filtered by approval_status (UC-D7) | Calls `GET /api/job-adjustment-requests?approval_status=approved` as `ma_staff`. | Response status is 200 and the auto-approved JAR's id is present among the results. |
| BE-SU-045 | Blocks a non ma_staff user from reviewing a JAR (403) (UC-D7) | Calls `PATCH /api/job-adjustment-requests/:id` as `vendor_liaison` (not `ma_staff`) trying to approve the material JAR. | Response status is 403. |
| BE-SU-046 | 404s reviewing a nonexistent JAR (UC-D7) | Calls the review PATCH endpoint against JAR id 999999 as `ma_staff`. | Response status is 404. |
| BE-SU-047 | 400s reviewing a JAR with an invalid approval_status (UC-D7) | Calls the review PATCH endpoint on the material JAR as `ma_staff` with `approval_status: 'pending_approval'` (not a valid decision value). | Response status is 400. |
| BE-SU-048 | Approves a pending JAR (UC-D7) | Calls the review PATCH endpoint on the material JAR as `ma_staff` with `approval_status: 'approved'`. | Response status is 200; `approval_status` is `approved`, and `approved_by`/`approved_at` are not null. |
| BE-SU-049 | 409s reviewing an already-decided JAR (UC-D7) | Re-calls the review PATCH endpoint on the now-approved material JAR with `approval_status: 'rejected'`. | Response status is 409. |
| BE-SU-050 | Rejects a separate pending JAR (UC-D7) | Creates a new material JAR (payment terms change), then calls the review PATCH endpoint on it as `ma_staff` with `approval_status: 'rejected'`. | Response status is 200; `approval_status` is `rejected` and `approved_by` is null. |
| BE-SU-051 | Blocks a non vendor_liaison user from creating a follow-up notification (403) (UC-D7) | Calls `POST /api/job-adjustment-requests/:id/follow-up-notification` as `ma_staff` on the approved material JAR. | Response status is 403. |
| BE-SU-052 | 404s creating a follow-up notification for a nonexistent JAR (UC-D7) | Calls the follow-up-notification endpoint against JAR id 999999 as `vendor_liaison`. | Response status is 404. |
| BE-SU-053 | 409s creating a follow-up notification for a JAR that is not approved (UC-D7) | Calls the follow-up-notification endpoint as `vendor_liaison` on the rejected JAR. | Response status is 409. |
| BE-SU-054 | Creates the follow-up notification log for an approved JAR (UC-D7) | Calls the follow-up-notification endpoint as `vendor_liaison` on the approved material JAR. | Response status is 201; `log_type` is `job_adjustment_notification` and `status` is `draft_ready`. |
| BE-SU-055 | 409s creating a second follow-up notification for the same JAR (UC-D7) | Re-calls the follow-up-notification endpoint on the same approved material JAR. | Response status is 409. |
| BE-SU-056 | Drafts the confirmation message on the job_adjustment_notification log (UC-D7) | Calls draft-message as `vendor_liaison` on the newly created `job_adjustment_notification` log. | Response status is 201; `subject` is "Confirmation of Adjusted Terms - TC-D-001", and `body` contains "Vendor Flagged Pte Ltd" and "written confirmation of the revised terms". |
| BE-SU-057 | 409s drafting a second message on the same job_adjustment_notification log (UC-D7) | Re-calls draft-message on the same follow-up log. | Response status is 409. |
| BE-SU-058 | Sets up a log in sent status via detect -> draft -> approve -> send (UC-D8/UC-D9) | Runs the full detect-deviation, draft-message, approve and send sequence as `ma_staff` for `tenderResendEscalate` with `dispatch_channel: 'manual'`. | The final send call returns status 200, leaving the log in `sent` status for use by later resend/escalate/resolve tests. |
| BE-SU-059 | 409s resending unless the log is sent (UC-D8/UC-D9) | Calls `POST /api/clarification-logs/:id/resend` as `ma_staff` on the `no_action_required` log (status is not `sent`). | Response status is 409. |
| BE-SU-060 | Blocks a non ma_staff/vendor_liaison user from resending (403) (UC-D8/UC-D9) | Calls resend as `evaluator` on the sent log. | Response status is 403. |
| BE-SU-061 | Resends a reminder and resets follow_up_due_at (UC-D8/UC-D9) | Calls resend as `ma_staff` on the sent log. | Response status is 201; `message_type` is `reminder` and `dispatch_channel` is `manual`; a follow-up GET shows `follow_up_due_at` matching a `YYYY-MM-DD` date pattern. |
| BE-SU-062 | 409s escalating a log that is not in sent status (UC-D8/UC-D9) | Calls `POST /api/clarification-logs/:id/escalate` as `ma_staff` on the flagged log, whose status is not `sent` (it is `responded` from earlier steps). | Response status is 409. |
| BE-SU-063 | Escalates a sent log to MA procurement staff (UC-D8/UC-D9) | Calls escalate as `ma_staff` on the sent log for `tenderResendEscalate`. | Response status is 200; `status` is `escalated`, and `escalated_by`/`escalated_at` are not null. |
| BE-SU-064 | 409s resolving a log that is not responded or escalated (UC-D8/UC-D9) | Calls `POST /api/clarification-logs/:id/resolve` as `ma_staff` on the `no_action_required` log with `outcome_notes: 'N/A'`. | Response status is 409. |
| BE-SU-065 | 400s resolving without outcome_notes (UC-D8/UC-D9) | Calls resolve on the escalated log as `ma_staff` with an empty request body. | Response status is 400. |
| BE-SU-066 | Resolves an escalated log (UC-D8/UC-D9) | Calls resolve on the escalated log as `ma_staff` with `outcome_notes` describing the resolution. | Response status is 200; `status` is `resolved`, `outcome_notes` matches the submitted text, and `resolved_by`/`resolved_at` are not null. |
| BE-SU-067 | Resolves a responded (not escalated) log too (UC-D8/UC-D9) | Calls resolve on the flagged log (which is `responded`, never escalated) as `vendor_liaison` with `outcome_notes`. | Response status is 200 and `status` is `resolved`. |
