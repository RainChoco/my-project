# Server
NODE_ENV=development
PORT=5000
FRONTEND_URL=http://localhost:5173

# Database (PostgreSQL - Neon in production, local Postgres/SQLite in dev)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=tender_db
DATABASE_URL=

# Auth
JWT_SECRET=
DEV_JWT_SECRET=dev-secret-tender-app

# Cloudinary (document storage)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ChatGPT API (OpenAI) - eligibility parsing, bid extraction, risk matrix, board paper/deck text, clarification drafting
OPENAI_API_KEY=
